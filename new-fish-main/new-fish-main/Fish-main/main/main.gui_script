local score = 0

function on_message(self, message_id, message, sender)
	score = score + 1
	if score%10 == 0 then
		--sound.play(find a cool sound effect)
	end
	gui.set_text(gui.get_node("score"), score)
	print("", message_id)
end